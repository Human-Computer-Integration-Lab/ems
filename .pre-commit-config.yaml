repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: 'v0.6.9'
    hooks:
      # run linter
      - id: ruff
        args: ["--fix", "--show-fixes"]

      # run formatter
      - id: ruff-format
